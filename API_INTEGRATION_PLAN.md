# План интеграции базы данных с приложением

## ✅ Выполнено:
1. ✅ Создана структура базы данных
2. ✅ Созданы все необходимые таблицы
3. ✅ Настроены связи между таблицами

## 📋 Следующие шаги:

### Шаг 1: Создание конфигурации и подключения к БД
- Создать `config.php` с настройками подключения
- Создать функцию подключения к базе данных

### Шаг 2: Создание API endpoints (PHP)

#### 2.1 Пользователи (users.php)
- `GET /api/users/get.php?telegram_id=XXX` - Получить пользователя
- `POST /api/users/create.php` - Создать/обновить пользователя
- `POST /api/users/update_activity.php` - Обновить активность

#### 2.2 Товары (products.php)
- `GET /api/products/list.php?category_id=XXX` - Список товаров по категории
- `GET /api/products/get.php?id=XXX` - Получить товар
- `GET /api/categories/list.php` - Список категорий

#### 2.3 Заказы (orders.php)
- `POST /api/orders/create.php` - Создать заказ
- `GET /api/orders/get.php?user_id=XXX` - Заказы пользователя
- `GET /api/orders/get.php?id=XXX` - Получить заказ

#### 2.4 Корзина (cart.php)
- `POST /api/cart/save.php` - Сохранить корзину
- `GET /api/cart/get.php?user_id=XXX` - Получить корзину

#### 2.5 Избранное (favorites.php)
- `POST /api/favorites/add.php` - Добавить в избранное
- `POST /api/favorites/remove.php` - Удалить из избранного
- `GET /api/favorites/get.php?user_id=XXX` - Получить избранное

#### 2.6 Отзывы (reviews.php)
- `GET /api/reviews/get.php?product_id=XXX` - Отзывы на товар
- `POST /api/reviews/create.php` - Создать отзыв

#### 2.7 Статистика (statistics.php)
- `GET /api/statistics/user.php?user_id=XXX` - Статистика пользователя
- `GET /api/statistics/online.php` - Онлайн пользователи

### Шаг 3: Интеграция API в frontend
- Создать JavaScript модуль для работы с API
- Заменить мок-данные на реальные запросы к API
- Реализовать сохранение заказов в БД
- Реализовать синхронизацию корзины и избранного

### Шаг 4: Импорт данных
- Импортировать существующие товары в БД
- Импортировать категории

### Шаг 5: Тестирование
- Тестирование всех API endpoints
- Тестирование интеграции с frontend
- Проверка сохранения данных

## 🎯 Рекомендуемый порядок выполнения:

**Приоритет 1 (Критично):**
1. Конфигурация и подключение к БД
2. API для товаров и категорий (загрузка данных)
3. API для создания заказов
4. Интеграция в frontend (загрузка товаров, сохранение заказов)

**Приоритет 2 (Важно):**
5. API для пользователей
6. API для корзины и избранного
7. API для статистики

**Приоритет 3 (Дополнительно):**
8. API для отзывов
9. Импорт данных
10. Оптимизация и кэширование

## 📁 Структура файлов для создания:

```
pc_salon/
├── api/
│   ├── config.php (подключение к БД)
│   ├── users.php
│   ├── products.php
│   ├── categories.php
│   ├── orders.php
│   ├── cart.php
│   ├── favorites.php
│   ├── reviews.php
│   └── statistics.php
├── js/
│   └── api.js (модуль для работы с API)
└── index.html (обновленный)
```

## ❓ Вопросы для уточнения:

1. С чего хотите начать? (API или интеграция)
2. Нужна ли аутентификация через Telegram WebApp сразу?
3. Какой формат ответов API предпочитаете? (JSON)
4. Нужна ли обработка ошибок и логирование?

